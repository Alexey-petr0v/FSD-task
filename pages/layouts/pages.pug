include ../../project.blocks/header-nav/header-nav
include ../../project.blocks/footer/footer

-
    var attr = [
        {logged: false}
    ]

doctype
html(lang='en')
    head
        meta(charset='utf-8')
        meta(name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0")
        block title
    body
        .wrapper
            +addHeaderNav(null, attr[0])
            block content
            +addFooter